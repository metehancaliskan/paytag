# ==========================================================================
# THIS REPO GOES PUBLIC AT DELIVERY. Nothing containing a secret belongs here.
# Git history cannot be undone: a secret committed once stays there forever.
# ==========================================================================

# --- Secrets --------------------------------------------------------------
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
secrets/
verifier-key*
verifier-secret*
*-secret.json
*-seed.txt

# --- Stellar CLI: identity files contain a seed phrase --------------------
.stellar/
.soroban/
.config/stellar/

# --- Rust / Soroban ------------------------------------------------------
contracts/target/
**/*.wasm

# --- Node / Next.js ------------------------------------------------------
node_modules/
web/.next/
web/out/
web/.vercel/
*.tsbuildinfo
.turbo/

# --- Test / build artifacts ----------------------------------------------
playwright-report/
test-results/
coverage/

# --- OS / editor ---------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp

# --- Local scratch --------------------------------------------------------
_to_delete/

# secret scanner test artifact
.scan-test-tmp/
