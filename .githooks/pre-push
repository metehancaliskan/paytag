#!/usr/bin/env bash
# Paytag pre-push — scans EVERY tracked file before a push.
# Last line of defence if a commit skipped pre-commit (--no-verify).
set -uo pipefail
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/scan-secrets.sh" --tree
