#!/usr/bin/env bash
# Paytag pre-commit — sır sızıntısını commit ANINDA durdurur.
# Kurulum: git config core.hooksPath .githooks   (scripts/setup-mac.sh yapar)
set -uo pipefail
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/scan-secrets.sh" --staged
