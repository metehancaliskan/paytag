#!/usr/bin/env bash
# Paytag pre-commit — stops a secret leak AT COMMIT TIME.
# Install: git config core.hooksPath .githooks   (scripts/setup-mac.sh does this)
set -uo pipefail
ROOT="$(git rev-parse --show-toplevel)"
exec "$ROOT/scripts/scan-secrets.sh" --staged
